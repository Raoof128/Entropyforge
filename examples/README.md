# Examples

These scripts use the library without starting the full GUI. Install the package
first:

```bash
pip install -e ..
```

From the `examples/` directory:

```bash
python generate_once.py
```

Requires the same Python environment as the main application.
